on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  Use-node:
    uses: ./.github/workflows/node-build.yml
  Tests-unit:
    runs-on: ubuntu-latest
    steps:
      needs: Use-node
      - name: Run unit tests 🧪
        run: npm run test:unit:ci
