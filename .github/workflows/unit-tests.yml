on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  Unit-tests:
    runs-on: ubuntu-latest
    uses: ./.github/workflows/ci.yml
    steps:
      - name: Run unit tests 🧪
        run: npm run test:unit:ci
