import { StyleSheet } from 'react-native';

export const spacing = StyleSheet.create({
  title: {
    marginBottom: 30,
  },
  input: {
    marginBottom: 25,
  },
  button: {
    marginTop: 40,
    marginBottom: 40,
  },
  link: {
    marginTop: 10,
    alignItems: 'flex-end',
  },
});
