import { StyleSheet } from 'react-native';

export const spacing = StyleSheet.create({
  input: {
    marginBottom: 25,
  },
  inputGroup: {
    marginBottom: 30,
  },
  button: {
    marginTop: 40,
    marginBottom: 40,
  },
});
